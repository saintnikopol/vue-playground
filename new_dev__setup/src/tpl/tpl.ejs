<html>
<head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
    <meta http-equiv="expires" content="0">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <!--0<link href="./css.css" rel="stylesheet">-->
    <title>Список книг</title>
</head>
<body>
<div id="app" class="row ">
<div class="header">
    <h4>Книги которые надо прочитать</h4>
</div>

    <div class="nav col-md-4">
        <h2>Список книг</h2>
        <ol>
            <li v-for="todo in visibleList" class="list">
                <router-link :to="'/authors/' + todo.author + '/books/' + todo.booksName + '/id/' + todo.id" v-bind:id="todo.id"
                             v-bind:booksName="todo.booksName">{{todo.booksName}}
                </router-link>

                <router-link :to="'/authors/' + todo.author" v-bind:id="todo.id"
                             v-bind:author="todo.author">{{todo.author}}
                </router-link>
            </li>

        </ol>
        <paginate
                :page-count="10"
                :page-range="3"
                :margin-pages="1"
                :click-handler="clickCallback"
                :prev-text="'Prev'"
                :next-text="'Next'"
                :container-class="'pagination'"
                :page-class="'page-item'">
        </paginate>
    </div>
    <div class="view col-md-8 text-center">
        <router-view></router-view>
    </div>

</div>
<div class="footer">

</div>

</body>

</html>
