<html>
<head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
    <meta http-equiv="expires" content="0">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <!--0<link href="./css.css" rel="stylesheet">-->
    <title>Список книг</title>
    <style>
        .header{
            background-color: #666;
            padding: 30px;
            text-align: center;
            font-size: 35px;
            color: white;
        }
        .nav {
            float: left;
            width: 40%;
            height: 70%; /* only for demonstration, should be removed */
            background: #ccc;
            padding: 20px;
        }
        h2{
            text-align: center
        }
        .view{
            float: right;
            padding: 20px;
            width: 50%;
            background-color: #f1f1f1;
            height: 70%;
            text-align: center;
            /*align-content: center;*/
        }
        a{
            padding-left: 2%;
            padding-right: 2%;
        }
        .list {
            padding-left: 5%;
            padding-right: 5%;
        }
        footer {
            background-color: #777;
            padding: 10px;
            text-align: center;
            color: white;
        }


    </style>
</head>
<body>
<div id="app" class="row ">
<div class="header">
    <h4>Книги которые надо прочитать</h4>
</div>

    <div class="nav col-md-4">
        <h2>Список книг</h2>
        <ol>
            <li v-for="todo in bookList" class="list">
                <router-link :to="'/authors/' + todo.author + '/books/' + todo.booksName + '/id/' + todo.id" v-bind:id="todo.id"
                             v-bind:booksName="todo.booksName" v-on:click="idBookFind()">{{todo.booksName}}
                </router-link>

                <router-link :to="'/authors/' + todo.author" v-bind:id="todo.id"
                             v-bind:author="todo.author">{{todo.author}}
                </router-link>
            </li>
        </ol>
    </div>
    <div class="view col-md-8 text-center">
        <router-view></router-view>
    </div>

</div>
<div class="footer"></div>

</body>

</html>
